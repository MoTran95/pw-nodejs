/**
 * 1 Get input by using getUserInput method
 * 2. Ask a function 1 to process the input data
 * 3. Ask function2 to validate the processed data
 * 
 */
let inputData = getUserInput();
let processedData = fillterNumberOnly (inputData);
verifyInputData(processedData);


// Function declaration below
function getUserInput(){
    //....
}
function fillterNumberOnly(inputData){
    //...
}