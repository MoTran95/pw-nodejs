let count;
for (count = 0; count <5; count++){
    // For loop body
    console.log(`Count: ${count}`);
}
console.log(`Final count's value: ${count}`);
/**
 * count = 0;  <--- ONLY ONE TIME
 * 
 * 
 *  What is the final value of the count variable above
 */
